import { div } from "framer-motion/client";
import React from "react";

const CheckoutPage = () => {
    return (
        <div className="container" style={{paddingTop: '5rem'}}>
            <h1>Checkout Page</h1>
            <p>This is where the shopping cart and checkout process will happen.</p>
        </div>
    );
};

export default CheckoutPage;